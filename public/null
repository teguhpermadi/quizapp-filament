$test = collect([1,2,3,4,5]);
$test = collect([1,2,3,4,5]);
$test->slice(2);
$test = collect([1,2,3,4,5]);
$test->filter(2);
$collection = collect([1, 2, 3, 4]);
 
$filtered = $collection->filter(function (int $value, int $key) {
    return $value > 2;
});
 
$filtered->all();
$collection = collect([1, 2, 3, 4]);
 
$filtered = $collection->filter(function (int $value, int $key) {
    return $value > 2;
});
 
$filtered->all();

$collection
$collection = collect([1, 2, 3, 4]);
 
$filtered = $collection->filter(function (int $value, int $key) {
    return $value > 2;
});
 
$filtered->all();


$collection = collect([1, 2, 3, 4]);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value > 2;
});
 
$filtered->all();
$collection = collect([1, 2, 3, 4]);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 2;
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 2;
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value === 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value === 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);
 
$filtered = $collection->reject(function (int $value, int $key) {
    return $value == 'a';
});
 
$filtered->all();
$collection = collect([1 => 'a', 2 => 'b', 3 => 'c', 4 => 'd']);

$updatedCollection = $collection->reject(function ($value, $key) {
    return $value === 'a';
});
